# -*- coding: utf-8 -*-

from discord.ext import commands
import discord

class Mod:
    """The description for Mod goes here."""

    def __init__(self, bot):
        self.bot = bot

def setup(bot):
    bot.add_cog(Mod(bot))
